--Looking at accreditation status for schools in VIRGINIA for a given year.
--
-- Parameters: sch_accred, school_year
--

SELECT sch_name, sch_accred AS sch_accred_status
FROM accreditation
WHERE sch_accred = 'Fully Accredited'
  AND school_year = '2008-2009';
